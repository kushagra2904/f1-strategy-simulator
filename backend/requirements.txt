# Web framework + server
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.0

# Data / ML
numpy>=1.26
pandas>=2.0
joblib>=1.3
# Pinned: the model artifacts (tire_degradation_model.pkl, compound_encoder.pkl)
# were trained with scikit-learn 1.6.1. Loading them under a different version
# raises InconsistentVersionWarning and may give invalid predictions.
scikit-learn==1.6.1
